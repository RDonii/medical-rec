SECRET_KEY=really-long-top-secret-key-example
HOST=127.0.0.1

# Warning: Delete or set False in production
DEBUG=False

# Required frontend data to implement password reset using email
# confirm urls should contain {uid} and {token} placeholders, e.g. #/username-reset/{uid}/{token}.
FRONTEND_PASSWORD_RESET_CONFIRM_URL=https://example.com/password-reset/{uid}/{token}
FRONTEND_USERNAME_RESET_CONFIRM_URL=https://example.com/username-reset/{uid}/{token}

# Database configurations
DB_NAME=medhub
DB_PASSWORD=cartest
DB_USER=cartest
DB_PORT=5434
DB_HOST=192.168.13.147
